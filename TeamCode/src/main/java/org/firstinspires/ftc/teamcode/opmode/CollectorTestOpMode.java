package org.firstinspires.ftc.teamcode.opmode;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

public class CollectorTestOpMode extends LinearOpMode {
    @Override
    public void runOpMode() {
        // declare collector

        /*
        write while loop containing
        - use gamepad keys to change collector state (should be able to go intake, extake, and turn off collector)
        - updates collector by calling update function
        - call telemetry.update() so collector telemetry appears to screen
        */

    }
}